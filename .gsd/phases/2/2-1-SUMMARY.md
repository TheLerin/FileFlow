# Plan 2.1 Summary

**Objective:** Install core dependencies for PDF manipulation and build the shared UI components.

**Completed Tasks:**
- Installed `pdf-lib` via npm.
- Created `src/lib/pdf-utils.ts` with the `downloadBlob` utility.
- Built a reusable natively-handled `FileUploadArea` component for drag-and-drop file selection with elegant Tailwind styling.

**Files Created/Modified:**
- `package.json`
- `src/lib/pdf-utils.ts`
- `src/components/ui/FileUploadArea.tsx`
